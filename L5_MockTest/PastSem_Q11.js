//Create a function sumPositiveNumbers that takes an array of numbers and returns the sum of only the positive numbers.

function somePositiveNumbers(someArr){
    let sum = 0;
    if (someArr.length === 0){
        return "Hey it's empty";
    }
    for (){
        if (someArr[currentElement])
    }

}